# Required only for the explicit live metered extraction command. Never commit a value.
OPENAI_API_KEY=

# Required for every web build. The date uses YYYY-MM-DD.
PUBLICATION_AS_OF_DATE=

# Required for every web build. Supply the owner's complete serviceable postal address.
LEGAL_ADDRESS=
