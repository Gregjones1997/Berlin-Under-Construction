schema_version = "1.0.0"
pricing_reference = "openai-gpt-5.6-luna-2026-08-07"
provider = "openai"
model = "gpt-5.6-luna"
currency = "USD"
source_url = "https://developers.openai.com/api/docs/pricing"
observed_on = "2026-08-07"

# Standard short-context rates per one million tokens. Inputs above 272k tokens
# use different rates and are prohibited by the byte limit below.
[rates_per_million_tokens]
input = "0.20"
cached_input = "0.02"
cache_write_input = "0.25"
output = "1.20"

[run_limits]
max_input_bytes = 200000
max_output_tokens = 2000
cost_ceiling = "0.30"
